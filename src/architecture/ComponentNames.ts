/**
 * Auto-generated Component Names for Next.js project
 *
 * These names provide type-safe access to all interactive elements
 * discovered in the project. Use them with the testId helper:
 *
 * @example
 * import { testId } from '@supernal/interface/testing';
 * import { Blog } from './ComponentNames';
 *
 * // In component:
 * <button data-testid={testId(Blog.submitButton)}>Submit</button>
 *
 * // In test:
 * await page.locator(testId(Blog.submitButton)).click();
 *
 * Generated from: 20 components
 * Total interactive elements: 29
 */

import { createNames } from '@supernal/interface/browser';

/**
 * Route Component Names
 * Prefix: route
 */
export const Route = createNames('route', {
  itemTitle: 'itemTitle',
});

/**
 * Search Component Names
 * Prefix: search
 */
export const Search = createNames('search', {
  form: 'form',
  button: 'button',
  link: 'link',
});

/**
 * BlogCard Component Names
 * Prefix: blogCard
 */
export const BlogCard = createNames('blogCard', {
  link: 'link',
});

/**
 * BlogGrid Component Names
 * Prefix: blogGrid
 */
export const BlogGrid = createNames('blogGrid', {
  button: 'button',
  button2: 'button2',
});

/**
 * BlogLayout Component Names
 * Prefix: blogLayout
 */
export const BlogLayout = createNames('blogLayout', {
  link: 'link',
});

/**
 * FolderCard Component Names
 * Prefix: folderCard
 */
export const FolderCard = createNames('folderCard', {
  link: 'link',
});

/**
 * FolderListView Component Names
 * Prefix: folderListView
 */
export const FolderListView = createNames('folderListView', {
  link: 'link',
  link2: 'link2',
});

/**
 * Header Component Names
 * Prefix: header
 */
export const Header = createNames('header', {
  button: 'button',
  link: 'link',
});

/**
 * MobileBottomNav Component Names
 * Prefix: mobileBottomNav
 */
export const MobileBottomNav = createNames('mobileBottomNav', {
  toggleChapterIndex: 'toggleChapterIndex',
  shareThisPost: 'shareThisPost',
});

/**
 * PostCard Component Names
 * Prefix: postCard
 */
export const PostCard = createNames('postCard', {
  link: 'link',
});

/**
 * PostComponent Component Names
 * Prefix: postComponent
 */
export const PostComponent = createNames('postComponent', {
  link: 'link',
});

/**
 * PostListView Component Names
 * Prefix: postListView
 */
export const PostListView = createNames('postListView', {
  link: 'link',
  link2: 'link2',
});

/**
 * RelatedPosts Component Names
 * Prefix: relatedPosts
 */
export const RelatedPosts = createNames('relatedPosts', {
  link: 'link',
});

/**
 * SearchBar Component Names
 * Prefix: searchBar
 */
export const SearchBar = createNames('searchBar', {
  form: 'form',
  form2: 'form2',
});

/**
 * ShareModal Component Names
 * Prefix: shareModal
 */
export const ShareModal = createNames('shareModal', {
  button: 'button',
});

/**
 * SocialShare Component Names
 * Prefix: socialShare
 */
export const SocialShare = createNames('socialShare', {
  link: 'link',
  originallyPublishedAt: 'originallyPublishedAt',
});

/**
 * Subscribe Component Names
 * Prefix: subscribe
 */
export const Subscribe = createNames('subscribe', {
  form: 'form',
});

/**
 * TagFilter Component Names
 * Prefix: tagFilter
 */
export const TagFilter = createNames('tagFilter', {
  button: 'button',
});

/**
 * FeaturedPosts Component Names
 * Prefix: featuredPosts
 */
export const FeaturedPosts = createNames('featuredPosts', {
  link: 'link',
});

/**
 * StoryGraphTimeline Component Names
 * Prefix: storyGraphTimeline
 */
export const StoryGraphTimeline = createNames('storyGraphTimeline', {
  input: 'input',
});
